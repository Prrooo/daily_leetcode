#include <algorithm>
#include <bits/stdc++.h>
using namespace std;

string getPermutation(int n, int k) {
  vector<int> arr;
  int temp = 1;
  for (int i = 0; i < n; i++) {
    arr.push_back(temp++);
  }
  temp = 0;
  do {

  } while (next_permutation(arr.begin(), arr.end()));
}

int main() { return 0; }

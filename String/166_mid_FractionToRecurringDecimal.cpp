#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
  string fractionToDecimal(int num, int den) {
    double fract = (double)num / den;
    cout << fract << endl;
    return "";
  }
};

int main() { return 0; }
